/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['source.unsplash.com', 'via.placeholder.com', 'www.youtube.com', 'placehold.co'],
  },
};

module.exports = nextConfig;
